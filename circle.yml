machine:
    java: 
        version: oraclejdk8
    
general:
    artifacts:
        - ui/build/distributions

dependencies:
    pre:
        - pip install --user codecov

test:
    post:
        - codecov
        - ./gradlew distZip

        
    