group = 'ro.samlex'


subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    sourceCompatibility = 1.7
    version = '0.9-SNAPSHOT'

    repositories {
        mavenCentral()
    }

    jar {
        manifest.attributes provider: 'gradle'
    }
}